describe('Use Case', () => {

    beforeEach(() => {
        //se ejecuta antes de cada test
    })

    afterEach(() => {
        //Se ejecuta despues de cada test
    })

    beforeAll(() => {
        //Se ejecuta antes de todos los tests
    })

    afterAll(() => {
        //Se ejecuta despues de todos los tests
    })

    test('Should to do something...', () => {
        //Cuerpo del test
    })

})